@page
@model Proiect_WebApp.Pages.Teachers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Teacher[0].Nume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teacher[0].Prenume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teacher[0].Telefon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teacher[0].Data_angajarii)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teacher[0].Scoala)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teacher[0].MaterieID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Teacher) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prenume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data_angajarii)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Scoala)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaterieID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
